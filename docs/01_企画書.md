# 企画書 v1.1 — 位置共有アプリ「Mimamori（みまもり）」

> 家族・友達とリアルタイムで位置を共有し、安全を見守るWebアプリ。
> 日本企業向けポートフォリオ作品。

---

## 1. プロジェクト概要

| 項目 | 内容 |
|---|---|
| **アプリ名** | **Mimamori（みまもり）** ※仮確定 |
| **一言で** | 家族・友達の「今どこ？」がひと目でわかる、プライベートな位置共有アプリ |
| **目的** | 日本企業就職用ポートフォリオ。技術的な見どころ = **リアルタイム通信（WebSocket）** |
| **ターゲット** | 家族（親子）、友達グループ、恋人・同居人 |
| **参考アプリ** | Life360（安全・構造）、Zenly（楽しい見た目）、Find My（シンプルさ） |

### コンセプト
「**見張る**」のではなく「**見守る**」。
監視ツールではなく、**お互いの合意**で位置を共有し、安心を分かち合うアプリ。

---

## 2. 機能一覧

### 2.1 コア機能（MVP — 必ず作る）

| # | 機能 | 説明 |
|---|---|---|
| F-01 | **リアルタイム位置共有** | グループメンバーの現在地を地図上にリアルタイム表示（WebSocket） |
| F-02 | **グループ（サークル）** | 招待コードでグループを作成・参加。グループ内だけで位置を共有 |
| F-03 | **共有ON/OFF** | いつでも自分の位置共有を停止できる（プライバシー最優先） |
| F-04 | **SOS発信** | ワンタップで正確なGPS位置つき緊急通報をグループへ即座に送信 |
| F-05 | **位置履歴** | メンバーの移動履歴を地図と時系列で確認 |
| F-06 | **場所（Places）と自動通知** | 「家」「学校」などを登録 → **到着/出発を自動検知して通知**（ジオフェンス） |
| F-07 | **距離表示** | 自分から相手までの距離をリアルタイム計算・表示 |
| F-08 | **バッテリー共有** | メンバーの電池残量を表示。低下時に通知 |
| F-09 | **通知一覧** | 到着・出発・SOS・電池低下などの履歴 |
| F-10 | **認証** | 会員登録・ログイン（JWT） |
| F-11 | **呼び出し（Ping）・応答確認** | 相手の端末で**警報音＋バイブ＋全画面表示**を鳴らし、応答を求める。子どもが電話に出ないときに使う。詳細は §2.4 |

### 2.2 拡張機能（余裕があれば）

| # | 機能 | 説明 |
|---|---|---|
| F-12 | チェックイン | 「着いたよ」を手動で送る |
| F-13 | 移動中ステータス | 徒歩/停止など簡易判定 |
| F-14 | 子ども向けモード | 文字を減らし、アイコンを大きくした簡易画面。**MVP対象外・後回し** |

### 2.3 F-11 呼び出し（Ping）・応答確認 — 詳細

**目的:** 子どもが電話に出ない・既読にならないとき、親が端末を強制的に鳴らして安否を確認する。

#### フロー
```
[親] メンバー詳細で「呼び出す」をタップ
   │  POST /api/pings  { targetUserId }
   ▼
[サーバー] ping を保存 → WebSocket と Web Push の両方で送信
   │
   ├─(A) アプリを開いている場合 → WebSocket 受信
   │        → 警報音ループ再生 ＋ バイブ ＋ 全画面オーバーレイ
   │          「お母さんが呼んでいます」［大丈夫だよ］ボタン
   │
   └─(B) アプリを閉じている場合 → Web Push → Service Worker
            → 通知表示（requireInteraction）＋ バイブ
              ※音は端末の標準通知音
   ▼
[子] ［大丈夫だよ］をタップ  → PUT /api/pings/{id}/respond
   ▼
[親] 「応答あり ✅」＋ そのときの位置を受信

⏰ 一定時間（既定3分）応答がない場合
   → 親に「応答なし ⚠️」を通知＋最終位置を表示（エスカレーション）
```

#### 画面
- **送信側（親）:** メンバー詳細（SC-M02）内の「呼び出す」ボタン → 送信後は応答待ち状態を表示
- **受信側（子）:** **SC-M04 呼び出し受信** — 全画面オーバーレイ。大きな文字・大きなボタン1つだけ

#### 実現可能性（正直に評価）

| ケース | 可否 | 内容 |
|---|---|---|
| アプリを開いている（前面・バックグラウンドタブ） | ✅ 実現可能 | WebSocket ＋ Web Audio API で警報音をループ再生。ログイン済み＝ユーザー操作済みなので autoplay 制限に当たらない |
| アプリを閉じている / 画面ロック中 | ⚠️ 一部のみ | Web Push ＋ Service Worker で通知とバイブは可能。ただし**カスタム警報音は鳴らせず、端末の標準通知音になる**（`Notification.sound` は実質未実装） |
| マナーモード / おやすみモードを無視して鳴らす | ❌ 不可能 | ネイティブアプリのみ可能。Web には権限がない |
| iOS / Safari | ⚠️ 条件付き | Web Push は **PWAをホーム画面に追加した場合のみ**動作（iOS 16.4以降）。通常の Safari タブでは受信不可 |

> 📌 **設計方針:** 「アプリを開いている間は確実に鳴る」を主機能とし、
> 閉じている場合は Web Push で「気づかせる」までを保証範囲とする。
> ネイティブアプリと同等の強制力は**Webでは実現できない**と明記する。

---

### 2.4 技術的制約（正直に明記）

> ⚠️ 本アプリは **Webアプリ（PWA）** のため、ネイティブアプリ（Life360等）と以下の差がある。
> 設計・デモ時はこれを前提とする。

| 制約 | 内容 | 対応 |
|---|---|---|
| **バックグラウンド追跡不可** | ブラウザを閉じる/画面ロックすると位置更新が止まる | アプリを開いている間のみ更新。最終更新時刻を必ず表示（「3分前」） |
| **Battery API 非対応ブラウザあり** | Firefox / Safari は `navigator.getBattery()` を削除済み。Chrome / Edge のみ対応 | 取得できない場合は「不明」と表示。機能の必須要件にしない |
| **GPS精度** | 屋内やWiFi測位では誤差が出る | 精度（accuracy）を保存し、誤差が大きい場合は地図に円で表示 |

---

## 3. 画面一覧（全13画面 / モバイルファースト）

> 命名規則: `SC-<区分><番号>`
> 区分: A=認証, M=メイン(地図), G=グループ, P=場所, S=SOS, T=設定

### 認証
| ID | 画面名 | 概要 | 優先度 |
|---|---|---|---|
| SC-A01 | ログイン | メール+パスワード | ★必須 |
| SC-A02 | 新規登録 | 名前・メール・パスワード | ★必須 |

### メイン（地図）
| ID | 画面名 | 概要 | 優先度 |
|---|---|---|---|
| SC-M01 | **地図（ホーム）** | 全画面地図＋メンバーのアバターピン＋下部シート（メンバー一覧）＋SOSボタン。**アプリの中心画面** | ★必須 |
| SC-M02 | メンバー詳細 | 下部シート内。現在地・距離・電池・最終更新・履歴へのリンク | ★必須 |
| SC-M03 | 位置履歴 | 日付を選び、その日の移動を地図＋時系列で表示 | ★必須 |
| SC-M04 | **呼び出し受信** | 全画面オーバーレイ。警報音＋バイブ＋［大丈夫だよ］ボタン1つ（F-11） | ★必須 |

### グループ
| ID | 画面名 | 概要 | 優先度 |
|---|---|---|---|
| SC-G01 | グループ一覧 | 参加中グループ、メンバー数、招待コード | ★必須 |
| SC-G02 | グループ作成・参加 | 新規作成／招待コードで参加 | ★必須 |
| SC-G03 | グループ詳細・メンバー管理 | メンバー一覧、脱退、削除（管理者） | ★必須 |

### 場所（Places）
| ID | 画面名 | 概要 | 優先度 |
|---|---|---|---|
| SC-P01 | 場所一覧 | 登録済みの「家」「学校」など | ☆推奨 |
| SC-P02 | 場所登録・編集 | 地図で位置と半径を指定（ジオフェンス） | ☆推奨 |

### SOS・通知
| ID | 画面名 | 概要 | 優先度 |
|---|---|---|---|
| SC-S01 | SOS発信 | 長押し3秒で発信。発信中は状態表示 | ★必須 |
| SC-S02 | SOS受信・詳細 | 受信時は全画面アラート。地図で相手の位置へ誘導 | ★必須 |
| SC-N01 | 通知一覧 | 到着/出発/SOS/電池低下の履歴。既読管理 | ★必須 |

### 設定
| ID | 画面名 | 概要 | 優先度 |
|---|---|---|---|
| SC-T01 | 設定・プロフィール | 位置共有ON/OFF、プロフィール編集、ログアウト | ★必須 |

**MVP = ★必須 12画面。** ☆推奨（Places 2画面）はF-06と一緒に実装。全14画面。

---

## 4. 技術構成

```
┌─────────────────┐   REST + WebSocket   ┌──────────────────┐
│  React + Vite   │ ───────────────────► │   Spring Boot    │
│   （Vercel）     │ ◄─────────────────── │   （Railway）     │
└─────────────────┘   STOMP over WS      └────────┬─────────┘
                                                   │
                                          ┌────────▼─────────┐
                                          │   PostgreSQL     │
                                          └──────────────────┘
```

### フロントエンド（`~/projects/ufo/frontend`）
| 項目 | 採用 |
|---|---|
| フレームワーク | React 19 + Vite 8（JavaScript / JSX） |
| ルーティング | react-router-dom 7 |
| スタイル | Tailwind CSS 4 + shadcn/ui |
| 状態管理 | zustand |
| HTTP | axios（`src/api/client.js` に集約） |
| **地図** | **Leaflet + react-leaflet**。タイルは **CARTO Positron**（昼）/ **Dark Matter**（夜）。APIキー不要・無料 |
| 逆ジオコーディング | **国土地理院（GSI）** API。キー不要・無料。住所はDBにキャッシュし約100m移動時のみ問い合わせ |
| WebSocket | `@stomp/stompjs` + `sockjs-client` |
| 位置取得 | `navigator.geolocation.watchPosition()` |
| 警報音・バイブ | Web Audio API + `navigator.vibrate()`（F-11） |
| プッシュ通知 | Service Worker + Web Push API（VAPID）（F-11） |
| デプロイ | **Vercel** |

### バックエンド（`~/projects/ufo/api` — これから作成）
| 項目 | 採用 |
|---|---|
| フレームワーク | **Spring Boot 3**（Java 21）, Maven |
| 認証 | Spring Security + **JWT**（HttpOnly Cookie で送受信） |
| リアルタイム | **Spring WebSocket + STOMP** |
| プッシュ送信 | `web-push` ライブラリ（VAPID）+ スケジューラ（無応答判定） |
| DB | **PostgreSQL**（地理計算・履歴に強い。将来 PostGIS 拡張も可） |
| ORM | Spring Data JPA |
| デプロイ | **Railway**（sister プロジェクトと同じ） |

> 📌 なぜ PostgreSQL？ 位置履歴という時系列＋地理データを扱うため。
> sister プロジェクトと同じ構成にして学習コストを下げる狙いもある。

---

## 5. DB設計（概要 / 10テーブル）

```
users            ユーザー
 ├─ group_members ─┬─ groups        グループ（サークル）
 │                 └─ places        登録場所（ジオフェンス）
 ├─ locations      位置履歴（時系列）
 ├─ place_events   到着/出発イベント
 ├─ sos_alerts     SOS通報
 ├─ pings          呼び出しと応答
 ├─ notifications  通知
 └─ push_subscriptions  Web Push 購読情報
```

| テーブル | 主なカラム |
|---|---|
| `users` | id, email, password_hash, name, avatar_color, created_at |
| `groups` | id, name, type, invite_code, created_by, created_at |
| `group_members` | id, group_id, user_id, role(OWNER/MEMBER), share_location(bool), joined_at |
| `locations` | id, user_id, lat, lng, accuracy, battery_level, recorded_at |
| `places` | id, group_id, name, icon, lat, lng, radius_m, created_by |
| `place_events` | id, place_id, user_id, event_type(ARRIVE/LEAVE), occurred_at |
| `sos_alerts` | id, user_id, group_id, lat, lng, status(ACTIVE/RESOLVED), triggered_at, resolved_at |
| `pings` | id, from_user_id, to_user_id, status(SENT/RESPONDED/NO_RESPONSE), sent_at, responded_at |
| `notifications` | id, user_id, type, title, body, is_read, created_at |
| `push_subscriptions` | id, user_id, endpoint, p256dh, auth, created_at ※Web Push用 |

**ポイント:**
- `locations` は履歴として全件保存 → F-05（位置履歴）に使う。**最新1件が現在地。**
- ジオフェンス判定はサーバー側で行う（位置受信時に `places` と距離計算 → 出入りが変わったら `place_events` + 通知）。
- 位置は **`share_location = true` のグループメンバーにしか配信しない**（F-03 プライバシー）。

---

## 6. リアルタイム設計（見どころ）

### WebSocket トピック
| トピック | 内容 |
|---|---|
| `/topic/group/{groupId}/location` | メンバーの位置更新 |
| `/topic/group/{groupId}/sos` | SOS発信・解除 |
| `/topic/group/{groupId}/place-event` | 到着/出発イベント |
| `/topic/group/{groupId}/presence` | オンライン・共有状態の変化 |
| `/user/queue/ping` | **自分宛の呼び出し**（個人宛なので topic ではなく user queue） |
| `/user/queue/ping-response` | 呼び出しへの応答・無応答の通知 |

### 位置更新の流れ
```
[ブラウザ] watchPosition()
    │  10〜30秒ごと / 一定距離移動時
    ▼
[POST /api/locations]  { lat, lng, accuracy, batteryLevel }
    │
    ▼
[Spring Boot]
    ├─ locations に保存
    ├─ places と距離判定 → 出入りがあれば place_events + notification
    └─ WebSocket でグループへ配信
           │
           ▼
[他メンバーのブラウザ] 地図のピンが動く
```

### 送信頻度の設計
- 通常: **30秒ごと**、または **20m以上移動**したとき
- SOS発動中: **5秒ごと**（正確な追跡が必要なため）
- 共有OFF時: **送信しない**

---

## 7. API設計（主要エンドポイント）

共通レスポンス形式: `{ success, data, message }`

| Method | Path | 説明 |
|---|---|---|
| POST | `/api/auth/register` | 新規登録 |
| POST | `/api/auth/login` | ログイン（JWT を HttpOnly Cookie で返す） |
| POST | `/api/auth/logout` | ログアウト |
| GET | `/api/auth/me` | 自分の情報 |
| GET | `/api/groups` | 参加中グループ一覧 |
| POST | `/api/groups` | グループ作成 |
| POST | `/api/groups/join` | 招待コードで参加 |
| GET | `/api/groups/{id}/members` | メンバーと最新位置 |
| DELETE | `/api/groups/{id}/members/{userId}` | 脱退・削除 |
| PUT | `/api/groups/{id}/share` | 位置共有 ON/OFF |
| POST | `/api/locations` | 現在地を送信 |
| GET | `/api/locations/{userId}/history?date=` | 位置履歴 |
| GET | `/api/places?groupId=` | 場所一覧 |
| POST | `/api/places` | 場所登録 |
| DELETE | `/api/places/{id}` | 場所削除 |
| POST | `/api/sos` | SOS発信 |
| PUT | `/api/sos/{id}/resolve` | SOS解除 |
| POST | `/api/pings` | 呼び出しを送る（F-11） |
| PUT | `/api/pings/{id}/respond` | 「大丈夫だよ」応答 |
| GET | `/api/notifications` | 通知一覧 |
| PUT | `/api/notifications/read` | 既読にする |
| POST | `/api/push/subscribe` | Web Push 購読登録 |

---

## 8. 開発ステップ

| # | フェーズ | 内容 | 状態 |
|---|---|---|---|
| 0 | 土台 | frontend 雛形（Vite+React+Tailwind+shadcn） | ✅ 完了 |
| 1 | 設計 | 本企画書 → 画面設計 → DB設計 → API設計 | 🔄 今ここ |
| 2 | フロント（モック） | 全画面を **mockApi** で作る。`// TODO [BACKEND]` を残す | ⬜ |
| 3 | バックエンド | Spring Boot + PostgreSQL、認証、CRUD | ⬜ |
| 4 | 結合 | mockApi → 実API に差し替え | ⬜ |
| 5 | リアルタイム | WebSocket（STOMP）で位置・SOS配信 | ⬜ |
| 6 | デプロイ | Vercel（front）+ Railway（api + DB） | ⬜ |

> **方針:** sister プロジェクトと同じく、まず **フロントをモックで完成**させてから backend を作る。
> こうすると画面が早く見えてモチベーションが保て、API仕様も固まりやすい。

---

## 9. 決定事項・未確定事項

### 決定済み
| 項目 | 決定 |
|---|---|
| アプリ名 | **Mimamori（みまもり）** ※仮確定。変更可 |
| バックエンド | Spring Boot 3 + PostgreSQL（Railway） |
| 技術的な見どころ | **リアルタイム（WebSocket / STOMP）** |
| 子ども向けモード（F-14） | **MVP対象外・後回し** |
| 呼び出し機能（F-11） | **MVPに追加**。Web の制約は §2.3 に明記 |

### 未確定
| 項目 | 状況 |
|---|---|
| **UIデザインの方向性** | 🔄 **保留中** — ユーザーが参考デザインを収集中。提示され次第、実現可能性を評価してから確定する。**急いで決めない**（本人の希望） |

> UI が決まるまでは、画面の**構造・機能・データの流れ**の設計を先に進める。
> 見た目（色・フォント・装飾）は後から差し替えられるよう、
> `index.css` のデザイントークンに集約しておく。
